@BeforeEach
void setUp() {
    hazelcastInstance.getMap("map-0").clear();
    hazelcastInstance.getMap("router-leader-map").clear();
    hazelcastInstance.getMap("router-heartbeat-map").clear();
    hazelcastInstance.getMap("router-claim-map").clear();
    hazelcastInstance.getReliableTopic("req-topic").destroy();
    hazelcastInstance.getReliableTopic("res-topic").destroy();
    testCollector.clear();
}
