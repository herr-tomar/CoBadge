Alles klar 👍 – ich habe die Passage zu den Response-Parametern entfernt und den gesamten Text ins Deutsche übertragen.

---

# Fachliche Spezifikation – Prüfung auf bestehende Reklamation

---

## 1. Detaillierte Beschreibung des Problems

Im **bestehenden Cobadge-Verfahren** prüft das System, ob bereits eine Reklamation existiert, ausschließlich über die **lokale Cobadge-Datenbank**. Alle Prüfungen (Duplikate, Error-Retry, Zeitfenster, Konsistenz, Berechtigung) werden datenbankseitig durchgeführt.

Im **neuen Verfahren** soll die *Existenzprüfung* direkt gegen das **IBO** erfolgen, das als interbankweites System of Record für Reklamationen gilt. Dadurch wird sichergestellt, dass Cobadge immer mit dem offiziellen externen Status abgeglichen ist und keine Abweichungen auftreten.

Dabei entstehen jedoch zwei wesentliche Probleme:

1. **Parameter-Mismatch**:

   * Dyns liefert transaktionsbezogene Kennungen (umsatzId, PAN, ARN usw.).
   * IBO benötigt jedoch issuerId und cardReference, um eine Reklamation nachzuschlagen.
   * Diese Parameter lassen sich nicht 1:1 abbilden.

2. **Prüfungen und Validierungen**:

   * Die alte Cobadge-DB führte zahlreiche technische und fachliche Prüfungen durch.
   * Diese Prüfungen sind über IBO nicht abgedeckt.
   * Es muss definiert werden, wie diese Validierungen künftig umgesetzt werden.

---

## 2. Dyns Request-Parameter

Aus der Java-Implementierung werden von Dyns an Cobadge folgende Felder übergeben:

* `umsatzId` → Transaktionskennung
* `transactionLifeCycleId` → Lebenszykluskennung der Transaktion
* `transactionDateAndTime` → Zeitstempel der Ausführung
* `transactionAmount` → Betrag der Transaktion
* `primaryAccountNumber (PAN)` → Kartennummer (bereinigt)
* `acquiringReferenceNumber (ARN)` → Referenznummer des Acquirers

---

## 3. IBO-Parameter

Der IBO-Service zur Prüfung einer bestehenden Reklamation lautet:

```
GET /api/v2/issuers/{issuerId}/cards/{cardReference}/disputes
```

### Erforderliche Eingabeparameter

* `issuerId` → Kennung des kartenausgebenden Instituts
* `cardReference` → Referenz auf die Karte (nicht identisch mit PAN)

---

## 4. Altes System – Prüfungen und Validierungen

Im bisherigen, rein datenbankbasierten Verfahren wurden folgende Prüfungen durchgeführt:

1. **Duplikatsprüfung**

   * Über UMSATZ\_ID, PAN, ARN in der Cobadge-DB.

2. **Error-Retry**

   * Bei vorherigem Fehlerstatus erneute Reklamation zulässig.

3. **Zeitfenster**

   * Transaktionsdatum maximal 540 Tage alt.

4. **Konsistenz**

   * Abgleich der Werte aus JSON (PAN, ARN, Betrag, Datum/Zeit) mit UMSATZ-Tabelle.

5. **Berechtigung**

   * Feld `CBD_UMS_REKL_ZLSG_KZ` = J/N entscheidet über Reklamationsfähigkeit.

---

## 5. Neues System – Vorgeschlagene Prüfungen und Validierungen

1. **Existenzprüfung**

   * Anstelle der Cobadge-DB erfolgt die Abfrage über den IBO-Endpunkt `/issuers/{issuerId}/cards/{cardReference}/disputes`.
   * Ergebnis entscheidet, ob eine Reklamation extern bereits existiert.

2. **Lokale Prüfungen**

   * Beibehaltung der datenbankseitigen Prüfungen:

     * Duplikatsprüfung
     * Error-Retry
     * Zeitfenster (≤ 540 Tage)
     * Konsistenz
     * Berechtigung (J/N-Kennzeichen)

3. **Clearing-Anforderung**

   * Auch im neuen Verfahren muss die Cobadge-Reklamationstabelle weiterhin gefüllt werden, da sie für Clearing-Prozesse benötigt wird, die nicht modernisiert werden.

---

## 6. Konkrete Herausforderungen

1. **Parameter-Mismatch**

   * Dyns liefert umsatzId/ARN/PAN; IBO benötigt issuerId + cardReference.
   * Es existiert aktuell keine direkte Abbildung.

2. **Anreicherungsproblem**

   * Um cardReference aus PAN abzuleiten, sind zusätzliche Mapping-Daten oder Services notwendig.
   * Bisher wurde keine verlässliche Lösung dafür identifiziert.

3. **Prüfungsabdeckung**

   * IBO liefert nur die Information, ob eine Reklamation existiert (über Statuscodes).
   * Cobadge-spezifische Prüfungen (Duplikat, Retry, Konsistenz, Berechtigung) müssen weiterhin lokal erfolgen.

---

## 7. Strategien zur Lösung des Problems

Drei Ansätze sind identifiziert:

1. **Dyns anpassen**

   * Dyns so erweitern, dass `issuerId` und `cardReference` direkt geliefert werden.

2. **Neuer IBO-Service**

   * Entwicklung oder Anforderung eines IBO-API-Endpunkts, der mit Dyns-Parametern (`umsatzId`, `ARN`, `PAN`) umgehen kann und die Umwandlung intern vornimmt.

3. **Anreicherung in Cobadge**

   * Dyns und IBO unverändert lassen.
   * In Cobadge eine Übersetzungs-/Anreicherungsschicht aufbauen, die:

     * `PAN → cardReference` abbildet,
     * `issuerId` ggf. aus DB ableitet,
     * weitere Parameter normalisiert,
   * und anschließend den IBO-Service mit den angereicherten Werten aufruft.

---


Natürlich 👍 – hier ein Vorschlag auf Deutsch für den JIRA-Eintrag:

---

**Summary**
Prüfung auf bestehende Reklamation über IBO anstelle Cobadge-DB – Parameter-Mapping umsetzen

**Description**
Im neuen Verfahren soll die Existenzprüfung von Reklamationen nicht mehr über die Cobadge-Datenbank, sondern direkt gegen IBO erfolgen.
Dyns liefert jedoch transaktionsbezogene Parameter (`umsatzId`, `transactionLifeCycleId`, `transactionDateAndTime`, `transactionAmount`, `PAN`, `ARN`), während IBO `issuerId` und `cardReference` benötigt. Dieses Parameter-Mapping ist derzeit nicht abgedeckt und verhindert den direkten Aufruf des IBO-Services.

Aufgabe:

* Strategie und Implementierung zur Auflösung des Parameter-Mismatches definieren.
* Sicherstellen, dass die Cobadge-DB weiterhin für Clearing befüllt wird und lokale Prüfungen (Duplikat, Error-Retry, Zeitfenster, Konsistenz, Berechtigung) bestehen bleiben.
* Umsetzung vorbereiten, sodass die Existenzprüfung über IBO erfolgt, während Cobadge-spezifische Validierungen lokal in der DB verbleiben.

---

Möchtest du, dass ich auch gleich einen **kurzen Epik-/Story-Kontext** formuliere (z. B. “Teil der Modernisierung des Reklamationsprozesses”), damit das Ticket im Backlog leichter eingeordnet werden kann?
